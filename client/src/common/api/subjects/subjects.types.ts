export type SubjectDto = {
    id: string
    name: string
    created_at: string
}

export type Subject = {
    id: string
    name: string
    createdAt: string
}

export type CreateSubjectPayload = {
    name: string
}

export type CreateSubjectDto = {
    name: string
}
