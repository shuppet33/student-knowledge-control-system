export type StudentDto = {
    id: string
    full_name: string
    email: string
    created_at: string
}

export type Student = {
    id: string
    fullName: string
    email: string
    createdAt: string
}
